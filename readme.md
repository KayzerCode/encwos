## EncWos (Encrypted Work Space)
Edge-stack Cloudflare: Pages + Workers + D1 + R2.

# Backend
# Run local DB
cd backend
pnpm run dev

# Run remote DB
cd backend
pnpm run dev --remote

# Frontend
cd frontend
pnpm run dev